# M5 板块管理 - Mock 模式快速指南

## 🎯 目的

让前端开发者可以在**没有后端服务**的情况下，独立开发和测试 M5 板块管理模块。

---

## ⚡ 3 步启动 Mock 模式

### 步骤 1：修改环境变量

打开 `.env.development` 文件，添加或修改：

```bash
VITE_USE_MOCK=true
```

### 步骤 2：启动开发服务器

```bash
npm run dev
```

### 步骤 3：验证 Mock 模式

打开浏览器控制台，应该看到：

```
🎭 [M5] 使用 Mock 数据模式
```

**完成！** 现在你可以完全独立测试前端功能了。

---

## 📱 可测试的功能

### 用户端功能

| 功能 | 路由 | 说明 |
|------|------|------|
| 板块列表 | `/sections` | 查看所有启用的板块 |
| 板块详情 | `/sections/tech` | 查看板块详细信息 |

### 管理员功能

| 功能 | 路由 | 说明 |
|------|------|------|
| 板块管理 | `/admin/sections` | 管理所有板块 |
| 创建板块 | 点击"添加板块" | 创建新板块 |
| 编辑板块 | 点击"编辑" | 修改板块信息 |
| 启用/禁用 | 点击"启用/禁用" | 切换板块状态 |
| 删除板块 | 点击"删除" | 删除板块 |

---

## 🎮 测试场景

### 场景 1：浏览板块列表

1. 访问 `http://localhost:3000/sections`
2. 应该看到 6 个板块卡片
3. 每个卡片显示图标、名称、描述、帖子数
4. 点击卡片可以进入详情页

**预期结果**：✅ 显示技术交流、设计分享等 6 个板块

---

### 场景 2：查看板块详情

1. 访问 `http://localhost:3000/sections/tech`
2. 应该看到"技术交流"板块的详细信息
3. 显示板块图标、名称、描述、帖子数、创建时间
4. 有"发布帖子"按钮（点击会提示待开发）

**预期结果**：✅ 显示完整的板块信息

---

### 场景 3：创建新板块

1. 访问 `http://localhost:3000/admin/sections`
2. 点击右上角"添加板块"按钮
3. 填写表单：
   - 板块标识：`frontend`
   - 板块名称：`前端开发`
   - 板块描述：`HTML、CSS、JavaScript`
   - 选择图标：⚡
   - 选择颜色：`#42A5F5`
   - 排序号：`7`
4. 点击"确定"
5. 应该看到成功提示
6. 新板块出现在列表中

**预期结果**：✅ 板块创建成功并显示在列表中

---

### 场景 4：编辑板块

1. 在板块管理页面，找到"技术交流"板块
2. 点击"编辑"按钮
3. 修改名称为"技术交流（新）"
4. 修改描述
5. 点击"确定"
6. 应该看到更新成功提示
7. 板块信息已更新

**预期结果**：✅ 板块信息更新成功

---

### 场景 5：禁用板块

1. 找到任意启用的板块
2. 点击"禁用"按钮
3. 板块卡片变灰，状态显示"禁用"
4. 访问 `/sections`，该板块不再显示
5. 回到管理页面，点击"启用"
6. 板块恢复正常

**预期结果**：✅ 板块状态切换成功

---

### 场景 6：删除板块

1. 找到"测试禁用板块"
2. 点击"删除"按钮
3. 弹出确认对话框
4. 点击"确定删除"
5. 板块从列表中消失

**预期结果**：✅ 板块删除成功

---

## 🔍 调试技巧

### 查看 Mock 数据

在浏览器控制台输入：

```javascript
// 查看所有板块
fetch('/api/sections').then(r => r.json()).then(console.log)
```

### 查看 API 调用

所有 API 调用都会在控制台输出日志：

```
[Mock API] 获取板块列表 {page: 1, page_size: 20, is_enabled: true}
[Mock API] 创建板块 {slug: 'frontend', name: '前端开发', ...}
```

### 重置数据

刷新页面即可重置所有 Mock 数据到初始状态。

---

## 📊 Mock 数据说明

### 预设板块（7个）

| ID | 标识 | 名称 | 图标 | 颜色 | 状态 | 帖子数 |
|----|------|------|------|------|------|--------|
| 1 | tech | 技术交流 | 💻 | #1976D2 | 启用 | 156 |
| 2 | design | 设计分享 | 🎨 | #9C27B0 | 启用 | 89 |
| 3 | discuss | 综合讨论 | 💬 | #4CAF50 | 启用 | 234 |
| 4 | question | 问答求助 | ❓ | #FF9800 | 启用 | 178 |
| 5 | share | 资源分享 | 📦 | #00BCD4 | 启用 | 92 |
| 6 | announce | 公告通知 | 📢 | #F44336 | 启用 | 23 |
| 7 | test-disabled | 测试禁用板块 | 🚫 | #9E9E9E | 禁用 | 0 |

### 数据特性

- ✅ 支持增删改查
- ✅ 数据验证（slug 和 name 唯一性）
- ✅ 模拟网络延迟（300ms）
- ✅ 错误处理
- ⚠️ 数据不持久化（刷新重置）

---

## 🔄 切换回真实 API

当后端服务准备好后：

### 步骤 1：修改环境变量

```bash
# .env.development
VITE_USE_MOCK=false
```

### 步骤 2：重启服务器

```bash
npm run dev
```

### 步骤 3：验证

控制台应该显示：

```
🌐 [M5] 使用真实 API 模式
```

---

## ⚠️ 注意事项

### 1. 数据不持久化

Mock 数据存储在内存中，刷新页面会重置。这是正常的，适合开发测试。

### 2. 模拟延迟

每个请求有 300ms 延迟，模拟真实网络环境。可以在 `sectionMock.js` 中调整。

### 3. 权限验证

Mock 模式下，所有管理员功能都可以访问（无需登录）。真实环境需要权限验证。

### 4. 图标字段

当前使用 emoji 作为临时图标方案，未来集成 M7 后会支持图片上传。

---

## 🎉 优势

### 为什么使用 Mock 模式？

1. **独立开发**
   - 不依赖后端服务
   - 前后端并行开发
   - 提高开发效率

2. **快速测试**
   - 即时反馈
   - 无需等待后端部署
   - 可控的测试数据

3. **演示友好**
   - 无需配置后端
   - 数据稳定可预测
   - 适合产品演示

4. **离线开发**
   - 无需网络连接
   - 随时随地开发
   - 不受后端服务影响

---

## 📂 相关文件

### Mock 实现文件

- `src/api/mock/sectionMock.js` - Mock 数据和 API 实现
- `src/api/modules/section.mock.js` - Mock API 封装
- `src/stores/modules/section.js` - 自动切换 Mock/真实 API

### 文档

- `src/api/mock/README.md` - 详细的 Mock 使用文档
- `MOCK-MODE-GUIDE.md` - 本快速指南

### 配置

- `.env.development` - 环境变量配置

---

## 🆘 常见问题

### Q1: 为什么看不到 Mock 数据？

**A**: 检查以下几点：
1. `.env.development` 中 `VITE_USE_MOCK=true`
2. 已重启开发服务器
3. 控制台显示 `🎭 [M5] 使用 Mock 数据模式`

### Q2: 如何添加更多测试数据？

**A**: 编辑 `src/api/mock/sectionMock.js`，在 `mockSections` 数组中添加更多板块。

### Q3: 数据为什么会重置？

**A**: Mock 数据存储在内存中，刷新页面会重置。这是设计行为，适合开发测试。

### Q4: 如何模拟错误场景？

**A**: Mock API 已经内置了常见错误处理：
- 板块不存在
- slug 重复
- name 重复

### Q5: 可以同时使用 Mock 和真实 API 吗？

**A**: 不建议。选择一种模式使用。可以通过环境变量快速切换。

---

## 📞 技术支持

如有问题，请查阅：
- [Mock 详细文档](./src/api/mock/README.md)
- [M5 模块文档](./src/views/sections/README.md)
- [前端架构文档](./ARCHITECTURE.md)

或联系前端开发团队。

---

**文档创建时间**：2025-11-16  
**维护者**：Open436 前端团队  
**状态**：✅ 可用

---

## 🚀 立即开始

```bash
# 1. 启用 Mock 模式
echo "VITE_USE_MOCK=true" >> .env.development

# 2. 启动开发服务器
npm run dev

# 3. 访问板块列表
# http://localhost:3000/sections

# 4. 访问管理后台
# http://localhost:3000/admin/sections
```

**祝开发愉快！** 🎉
